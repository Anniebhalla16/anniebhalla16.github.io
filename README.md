# Annie Bhalla — Portfolio

Personal landing page, built with Next.js 15 (App Router) and plain CSS/inline styles.

## Development

```bash
npm install
npm run dev
```

## Scripts

- `npm run dev` — start the dev server
- `npm run build` — production build
- `npm run start` — serve the production build
- `npm run lint` — lint the project

## Deployment

Deployed to Vercel via GitHub Actions (`.github/workflows/ci-cd.yml`) on push to `main`.

### Future Plans
1. MCP grounded on 'Annie'
2. Supabase Connection from where the data will come + security